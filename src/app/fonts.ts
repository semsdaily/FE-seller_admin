import { Inter, Lexend_Deca, Roboto, Roboto_Mono } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });
export const roboto = Roboto_Mono({ subsets: ['latin'], variable: '--font-roboto' });

export const lexendDeca = Lexend_Deca({
  subsets: ['latin'],
  variable: '--font-lexend',
});
